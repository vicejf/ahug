package nc.bs.<%= bill.module %>.pub.action;

import nc.bs.framework.common.NCLocator;
import nc.itf.<%= bill.module %>.I<%= bill.billCode %>;
import nc.vo.<%= bill.packageName %>.<%= bill.billCode.toLowerCase() %>.Agg<%= bill.billCode %>VO;
import nc.vo.pub.AggregatedValueObject;

/**
 * <%= bill.billName %> 保存动作
 * 创建日期:<%= date %>
 * @author <%= bill.globalConfig?.author || 'Unknown' %>
 */
public class N_<%= bill.billCode %>_SAVE {

    public AggregatedValueObject[] save(AggregatedValueObject[] billVOs) throws Exception {
        I<%= bill.billCode %> service = NCLocator.getInstance().lookup(I<%= bill.billCode %>.class);
        return service.save(billVOs);
    }
}
