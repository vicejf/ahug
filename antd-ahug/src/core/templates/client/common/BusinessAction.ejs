package nc.ui.<%= bill.packageName %>.<%= bill.billCode.toLowerCase() %>.action;

import nc.ui.uif2.NCAction;
import nc.ui.uif2.model.AbstractAppModel;

/**
 * <%= bill.billName %> 业务动作
 * 创建日期:<%= date %>
 * @author <%= bill.globalConfig?.author || 'Unknown' %>
 */
public class BusinessAction extends NCAction {

    private static final long serialVersionUID = 1L;
    private AbstractAppModel model;

    public BusinessAction(AbstractAppModel model) {
        super("业务操作");
        this.model = model;
        setCode("businessAction");
    }

    @Override
    public void doAction() {
        
    }

    @Override
    protected boolean isActionEnable() {
        return model.getSelectedData() != null;
    }
}
