package nc.bs.<%= bill.module %>.<%= bill.billCode.toLowerCase() %>;

import nc.bs.dao.BaseDAO;
import nc.bs.dao.DAOException;
import nc.vo.<%= bill.packageName %>.<%= bill.billCode.toLowerCase() %>.Agg<%= bill.billCode %>VO;
import nc.vo.pub.BusinessException;

/**
 * <%= bill.billName %> 规则类
 * 创建日期:<%= date %>
 * @author <%= bill.globalConfig?.author || 'Unknown' %>
 */
public class Rule {

    private static final long serialVersionUID = 1L;

    public void beforeInsert(Agg<%= bill.billCode %>VO billVO) throws BusinessException {
    }

    public void afterInsert(Agg<%= bill.billCode %>VO billVO) throws BusinessException {
    }

    public void beforeUpdate(Agg<%= bill.billCode %>VO billVO) throws BusinessException {
    }

    public void afterUpdate(Agg<%= bill.billCode %>VO billVO) throws BusinessException {
    }

    public void beforeDelete(String[] pkArray) throws BusinessException {
    }

    public void afterDelete(String[] pkArray) throws BusinessException {
    }
}
